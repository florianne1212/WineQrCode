<form method="post" action="{{ path('app_wine_delete', {'id': wine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wine.id) }}">
    <button class="btn">Delete</button>
</form>
