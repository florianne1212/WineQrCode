
{{ form_start(form) }}
<div class="wine-form">
    {{ form_widget(form,  { 'attr': {'class': 'my-form'} }) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}

